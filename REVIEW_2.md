# k8s-diagnose 第2回コードレビュー（大幅改善後）

対象: `github.com/kmizuki03/k8s-diagnose`
比較基準: 初回レビュー時点（`375cad3`）→ 現在（`8ae37f6` stage3）
変更規模: **105ファイル / +23,834行**（Go実装 14,785行 → 21,767行、テスト 3,222行 → 5,227行）
レビュー種別: 静的レビュー（コード読解）

> 注意: 前回同様、本環境にGo 1.25ツールチェーンを導入できず（ネットワーク遮断）、`go build` / `go test` / `staticcheck` / `gosec` は未実行です。以下はコード読解に基づく評価で、動的検証はCI結果と併せて確認してください。

---

## 総評

**前回の指摘6件が、すべて解消されています。** しかも単に潰しただけでなく、指摘の背景にあった課題まで踏み込んで設計し直しています。

さらに、`IDEA.md` で言語化した「説明書を読まなくても使えるようにしたい」という狙いが、実際に動くコードとして形になりました。対話ウィザード・サブコマンド・プリセット・設定エディタが揃い、**「入口を3層に分ける」という構想がそのまま実装に落ちています**。

特に評価したいのは、新機能を足しながら**既存の設計原則を一切崩していない**点です。ウィザードは `config.WithSetting` を通して既存の検証を再利用し、設定保存は既存のアトミック書き込み方式を踏襲、プリセットは `Validate` を必ず通す。新しい入口を作るときに検証をバイパスするのが最もありがちな失敗ですが、それを回避しています。

規模が1.5倍になっても構造が崩れていないので、増築ではなく設計に基づく拡張ができていると評価します。

---

## 1. 前回指摘への対応状況

| # | 前回の指摘 | 深刻度 | 対応 | 評価 |
|---|-----------|:---:|------|:---:|
| 1 | `-config`（シングルダッシュ）でINIが黙って無視される | 中 | `findConfigPath` が `-config` / `-config=` も検出（config.go:637-639） | ✅ 解消 |
| 2 | パーサ系にFuzzテストがない | 中 | 4本追加: `FuzzLoadINI` / `FuzzMaskSecrets` / `FuzzLoadLogAnalyzer` / `FuzzDecodeDocument` | ✅ 解消 |
| 3 | report/history間のヘルパ重複 | 低 | `internal/jsonutil` に共通化（`Objects` `StringField` `UnavailableSections` `MatchingUnavailable` `EvaluationUnavailable` 等） | ✅ 解消 |
| 4 | `app.go` 肥大（1,175行） | 低 | 12ファイルへ分割（app.go 557行、render/logs/debug/selection/commands/connect_ui 等） | ✅ 解消 |
| 5 | Makefileに `fclean` なし | 低 | `fclean`（dist・db・カバレッジ削除）と `re` を追加 | ✅ 解消 |
| 6 | バージョンが `3.0.0-dev` 固定 | 低 | `-X` でLDFLAGS注入、`scripts/version.sh` 追加 | ✅ 解消 |

補足として、以前会話で挙げた「API要求ダンプが既定で出て圧が強い」件も、`ShowAPIRequests` という独立フラグに切り出され、`quick`/`ci` プロファイルで明示的にオフにされています（command.go:82,87）。ただし `Defaults()` では `true` のままなので、既定オフにするかは判断が残ります（後述）。

---

## 2. 新機能の評価

### 2.1 対話ウィザード（`internal/app/wizard.go`, 1,086行）

構想どおり「段階的な絞り込み」が実装されています。

**良い点**

- **モード選択 → そのモードで有効なオプションのみ提示 → 詳細設定**という3段構成が、`configureAll` / `configurePod` で明確に分岐。全体診断では接続確認を出さず、Pod個別でのみ出す（wizard.go:196, 255）。狙いどおり、制約をエラーで教えるのではなく**選べないようにする**形になっています。
- **戻る操作が正しくスタックになっている**。`runGuidedSettingsFrom` で「詳細から戻ると1つ前の詳細へ、最初の詳細から戻るとオプション画面へ」を実装（wizard.go:321-342）。さらに確認画面から戻ると最後の詳細に復帰する（`resumeDetails`、wizard.go:135, 175）。ウィザードで最も手を抜かれやすい部分を作り込んでいます。
- **オプションOFF時に関連設定を消す**。`setLogs`/`setConnect`/`setDebug` が、無効化時に従属設定（tail、port、path、image、profile）の明示値を除去（wizard.go:344-381）。「一度ONにして設定し、OFFに戻したら値が残る」というゴミを防いでいて丁寧。
- **検証を再利用**。ウィザードが値を書くのは `config.WithSetting` 経由で、独自の検証を持たない（wizard.go:453）。対話・INI手編集・CLIすべてが同じ検証を通るという `IDEA.md` の原則が守られています。
- **端末制御が堅実**。`readWizardLine` がraw modeを使い、`defer` と `restored` フラグで確実に復元。Ctrl-C(0x03)/Ctrl-D(0x04)/Backspace/Ctrl-U を個別処理し、**マルチバイト文字を `utf8.DecodeLastRune` で1文字ずつ削除**（wizard.go:845）。日本語入力で崩れません。
- **エスケープシーケンスの読み捨て**（wizard.go:878-917）が特に良い。矢印キーやマウスホイールのCSI/SS3/OSC、さらに**X10マウス形式の後続3バイト**まで処理。これがないと設定値に `[A` や座標が混入します。実際に踏んだ問題を、コメント付きで正しく塞いでいる。

**気づいた点**

- `Guide` 関数のメインループ（wizard.go:82-179）が、ネスト3〜4段＋`resumeDetails` などのフラグで制御されており、追うのに集中力が要ります。動作は正しく読めますが、ここは将来の変更で壊しやすい箇所です。「今どの画面にいるか」を状態として持つ形（画面スタック）にすると見通しが良くなる余地があります。**現時点で不具合ではなく、複雑さの指摘**です。
- 選択肢のインデックスが数値リテラルで参照されています（`choice == 7` で終了、`choice == 6` で設定、`profiles[choice]`、wizard.go:104-124）。メニュー項目を1つ挿入すると、離れた場所の数値も直す必要があり、事故の芽になります。項目側に識別子（`id: "settings"` など）を持たせて名前で分岐する方が安全です。同様に `configureAll`/`configurePod` の `selected[0]` `selected[1]` `selected[2]` も位置依存です（wizard.go:220-296）。
- `drawMenu` が「ルートメニューかどうか」を **タイトル文字列の一致**で判定しています（`rootMenu := title == rootWizardTitle`、wizard.go:578）。定数化されているので現状は動きますが、表示文言と制御ロジックが結合しており、文言変更で挙動が変わり得ます。フラグ引数で渡す方が安全です。

### 2.2 サブコマンドとプリセット（`internal/config/command.go`）

**良い点**

- `quick` / `ci` / `deep` / `all` / `pod` / `list` / `triage` / `config` / `advanced` を1語で選べるようになり、狙いどおり入口が簡潔になりました。
- **既存のフラグ運用を壊していない**。`CommandName` は先頭引数が `-` で始まればコマンドなしと判定するため（command.go:19）、従来の `-a --logs` 形式がそのまま動きます。移行の逃げ道が確保されている点が良い。
- **適用順序が明確**。「INI読込 → コマンドプロファイル → 明示的CLIフラグ」の順で重ねる設計（command.go:54のコメント）。プリセットが最優先ではなく、ユーザーの明示指定が勝つのは正しい優先順位です。
- `prepareMode` が、モードで意味を持たない設定だけをリセットし、**target/API系（namespace, context, qps等）は保持**する（command.go:54-56）。「プリセットにしたら接続先設定まで飛んだ」という事故を避けています。
- `ApplyProfile` が必ず `Validate` を通す（command.go:257）。ウィザードから来た組み合わせも検証されるので、対話UIが不正な設定を生成できません。
- `config` サブコマンドが**設定値フラグを受け付けない**（`validateConfigEditorArgs`、command.go:99）。「対話で編集するモード」に中途半端なフラグ指定が混ざらないよう明確に分離していて、意図が一貫しています。

**気づいた点**

- `prepareMode` と各 `clear*` 関数に、リセット対象の設定キー名が**文字列リテラルで大量に列挙**されています（command.go:140-177）。新しい設定を追加したとき、ここへの追記漏れに気づく仕組みがありません。`settings_catalog.go` にカタログがあるので、「このモードで有効なキー」をカタログ側の属性として持たせ、リセット対象を導出できると堅牢になります。**現時点の実害はなく、拡張時のリスク**です。
- `applyCommandProfile` の `quick` と `ci` はリセット内容がほぼ同一で、差は出力形式と `events_limit` のみ（command.go:78-88）。共通部分を括り出せます（可読性の話）。

### 2.3 設定の対話編集と保存（`SaveINI`, `settings_catalog.go`）

**良い点**

- **`IDEA.md` の「対話でもファイル直編集でも同じ」が実現されています**。対話編集は `applyPromptedSetting` → `config.WithSetting`（既存の検証）→ `SaveINI` という経路で、手編集INIと同じ `knownSetting`/`applySetting` を通ります。
- **保存がアトミック**。一時ファイル → `Chmod(0600)` → Write → Sync → Rename → 親ディレクトリfsync（settings_catalog.go:285-345）。既存の `report.WriteAtomic` と同じ品質で、設定ファイルにも同水準の堅牢性を適用したのは一貫していて良い。**0600で保存**しているのも、設定にwebhook環境変数名などが入り得ることを考えると妥当です。
- 設定項目をカタログ（`SettingCatalog`）で一元管理し、ウィザードのセクション分けをそこから導出（wizard.go:1044-1064）。項目追加がメニューに自動反映されます。
- boolean設定は自由入力ではなく**3択メニュー**（有効/無効/既定に戻す、wizard.go:1005）。「trueって書くのかyesなのか」で迷わせない配慮。

**気づいた点**

- 設定は**Enter確定ごとに即INIへ保存**されます（wizard.go:994-1001、画面にも明示）。取り消し（キャンセルして破棄）ができないため、試しに変えてみる操作ができません。挙動として明示されているので誤解は生みませんが、「最後にまとめて保存/破棄」を選べる方が安心感はあります。設計判断の範囲です。
- `SaveINI` は書き込み時に既存ファイルを**丸ごと再生成**します（`RenderINI`）。ユーザーが手で書いたコメントや並び順は保持されない可能性があります。「対話とファイル編集は対等」という原則からすると、手編集派のコメントが消えるのは軽い摩擦になり得ます。実害は小さいですが、READMEに一言あると親切です。

### 2.4 Podスコア（`internal/app/pod_score.go`, 494行）

- **クラスタHealthとPodスコアを明確に区別**し、その理由をコメントで説明（pod_score.go:13-16）。「クラスタは独立した根本原因の数、Podは今どれだけ使えるか」という定義の違いが明快です。
- 12次元（ライフサイクル、Ready、コンテナ、再起動、リソース、スケジューリング、依存、Storage、Probe、Service、NetworkPolicy、Ingress/TLS）の合計が100で、**どの次元が減点したかが見える**設計。単一スコアより診断的価値が高い。
- 各次元が `Detail` を持ち、減点理由を文字列で提示。「なぜこの点数か」を説明できる点が良い。
- 未取得（`known == false`）を0点ではなく中間点（7点）にしている（pod_score.go:75）のは、「取得不能と異常を区別する」という既存の設計思想と一致していて一貫性があります。

---

## 3. 全体を通しての評価

### 一貫して守られている点

- **検証の一元化**: CLI・INI・ウィザードのすべてが `applySetting`/`Validate` に集約。入口が3つに増えても検証は1つ。
- **既存互換**: 旧フラグ形式が動き続ける。破壊的変更を避けつつ新UIを追加できています。
- **安全設計の継承**: 新規に増えた書き込み処理（SaveINI）にも、既存のアトミック書き込み・0600・fsyncの水準がそのまま適用されています。新機能でセキュリティ水準が下がっていない。
- **テストの追随**: テストが3,222→5,227行（+62%）。新機能（`wizard`周辺の`select_scope_test`、`pod_score_test`、`diagnostic_contents_test`）にもテストが付いており、機能追加とテスト追加が同時に行われています。

### 残る改善候補（すべて軽微）

| # | 内容 | 深刻度 | 備考 |
|---|------|:---:|------|
| A | ウィザードのメニュー分岐が数値インデックス依存（`choice == 7`、`selected[0]`） | 低 | 項目追加時の事故防止に識別子化を推奨 |
| B | `Guide` のメインループが多段ネスト＋フラグ制御で複雑 | 低 | 画面スタック化で見通し改善の余地 |
| C | `drawMenu` がタイトル文字列でルート判定 | 低 | 表示文言と制御の結合。引数化を推奨 |
| D | `prepareMode`/`clear*` の設定キー列挙が手動管理 | 低 | 設定追加時の追記漏れリスク。カタログ由来にできると堅牢 |
| E | `ShowAPIRequests` の `Defaults()` が `true` | 低 | プリセットではオフだが、素の `-a` では出る。既定オフ＋`--cmd`で表示が親和的 |
| F | 設定編集が即時保存で取り消し不可 | 低 | 明示されているので誤解はない。設計判断 |
| G | `SaveINI` がINIを再生成（手書きコメントが残らない可能性） | 低 | READMEに明記があると親切 |
| H | `quick`/`ci` プロファイルの重複 | 低 | 可読性のみ |

**重大な問題は見つかりませんでした。** A〜Hはいずれも「今壊れている」ものではなく、将来の変更で壊れにくくするための提案です。

---

## 4. 動的検証の推奨（本レビューで未実施）

```
make verify
go test -race ./...
go test -run Fuzz -fuzz FuzzLoadINI -fuzztime 30s ./internal/config/
go test -run Fuzz -fuzz FuzzMaskSecrets -fuzztime 30s ./internal/redact/
make lint
make security
./scripts/run-ci-tests.sh
```

対話UIは自動テストが難しい領域なので、以下は手動確認を推奨します。

- ウィザードで**日本語を入力**して Backspace（マルチバイト削除の確認）
- 入力欄で**矢印キー・マウスホイール**を操作（エスケープ混入がないこと）
- 詳細設定から `b` で戻る → 確認画面から戻る（スタック復帰の確認）
- ウィザード実行中に **Ctrl-C**（端末が raw mode のまま残らないこと。ここは復元処理が入っていますが実機確認の価値が高い）
- **パイプ経由**で実行（`echo | k8s-diagnose`）してウィザードが起動せず従来動作になること

---

## 5. 結論

前回指摘の6件は**全解消**。加えて `IDEA.md` で言語化した構想（3層の入口、対話とファイル編集の対等な扱い）が、設計原則を崩さずに実装されました。

規模が1.5倍になっても、検証の一元化・既存互換・安全設計という軸がぶれていないのが最大の評価点です。新しいUIを足すときに既存の検証をバイパスして独自実装してしまうのがよくある失敗ですが、`WithSetting` と `Validate` を必ず通す形を貫いたことで、入口が増えても信頼性は1つに保たれています。

残課題は「将来の変更に対する壊れにくさ」（数値インデックス、キー手動列挙）に集中しており、機能・安全性の問題ではありません。優先度としては A と D を、次に機能追加するタイミングで一緒に整理すれば十分です。

前回に続き、非常に良い改善だと思います。
